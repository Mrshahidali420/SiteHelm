# Tooling state — not part of the project
.remember/
.claude/
mut/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# SiteHelm build/tooling
vendor/
build/
.phpunit.result.cache
.superpowers/

# Agent workflow artifacts — kept locally, not part of the published plugin
tasks/
docs/superpowers/

# Agent harness state — local tooling, not part of the plugin
.github/agents/
.github/hooks/
.github/skills/

# Vendored reference copy — kept locally, never part of the plugin
/elementor mcp that i want you to copy from i have permission/

# Impeccable design-skill scratch state (critique snapshots, question logs)
.impeccable/
